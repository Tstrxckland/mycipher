# MyCipher

This project implements a simple Caesar Cipher in Python.

The program accepts a shift amount from the command line, reads text from standard input, converts letters to uppercase, removes punctuation/spaces/numbers, shifts each letter, and prints the encoded message in blocks of five letters.

## How to Run

```bash
python3 mycipher.py 3 < plaintext
